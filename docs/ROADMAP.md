# Roadmap

> Edited deliberately when priorities change — not automatically overwritten each session.

## Near-term

- Follow `docs/IMPLEMENTATION_PLAN.md` phases (see that file for the authoritative plan).

## Backlog

- Additional selector strategies: `NameRegex`, `ControlTypeIndex`, `Coordinates`.
- Navigation map support (see `docs/NAVIGATION_MAP_SCHEMA.md`).
